# Configuration for STARK proof generation workflow
# Copy this file to .env and adjust paths as needed

# Cairo toolchain - using wrapper script that handles venv
# If you have cairo-run in PATH globally, you can use: CAIRO_RUN=cairo-run
CAIRO_RUN=./scripts/cairo-run-wrapper.sh

# Stone prover directory (for cairo-run venv)
# Example: /path/to/stone-prover or ../stone-prover
STONE_PROVER_DIR=/path/to/stone-prover
VENV_NAME=venv39

# Cairo lang directory (for bootloader cairo-run)
# This should point to cairo-lang with bootloader support (cartridge fork)
# Example: /path/to/cairo-lang-latest or ../cairo-lang-latest
CAIRO_LANG_DIR=/path/to/cairo-lang-latest
CAIRO_LANG_VENV=venv39

# Programs (relative to project root)
CPU_AIR_PROVER=./programs/cpu_air_prover
CPU_AIR_VERIFIER=./programs/cpu_air_verifier

# Prover settings (relative to project root)
PROVER_CONFIG=./prover_settings/cpu_air_prover_config.json
PROVER_PARAMS=./prover_settings/cpu_air_params.json

# Ethereum verifier adapter
STARK_EVM_ADAPTER=stark_evm_adapter

# Working directory for outputs
WORK_DIR=./work

# Deployment Configuration
# Copy this file to .env.deploy and fill in your values

# ===== REQUIRED =====

# Private key for deployment (without 0x prefix)
# NEVER commit this file with real private key!
PRIVATE_KEY=your_private_key_here

# Ethereum Sepolia RPC URL (choose one)
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_API_KEY
# Or use Infura: https://sepolia.infura.io/v3/YOUR_API_KEY
# Or public: https://rpc.sepolia.org

# Base Sepolia (Testnet) RPC URL
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
# Or use Alchemy: https://base-sepolia.g.alchemy.com/v2/YOUR_API_KEY

# Base Mainnet RPC URL
BASE_RPC_URL=https://mainnet.base.org
# Or use Alchemy: https://base-mainnet.g.alchemy.com/v2/YOUR_API_KEY

# ===== OPTIONAL =====

# Etherscan API key for Ethereum contract verification (OPTIONAL)
# Get one at: https://etherscan.io/myapikey
ETHERSCAN_API_KEY=your_etherscan_api_key_here

# Basescan API key for Base contract verification (OPTIONAL)
# Get one at: https://basescan.org/myapikey
BASESCAN_API_KEY=your_basescan_api_key_here

# ===== PROOF VERIFICATION (Optional - defaults to examples) =====

# Path to annotated_proof.json (default: examples/factorial-bootloader/annotated_proof.json)
# If not set, will use example from examples/factorial-bootloader/
ANNOTATED_PROOF=examples/factorial-bootloader/annotated_proof.json

# Path to input.json (default: examples/factorial-bootloader/input.json)
# If not set, will use example from examples/factorial-bootloader/
INPUT_JSON=examples/factorial-bootloader/input.json

# Path to fact_topologies.json (default: bootloader/fact_topologies.json)
# If not set, will use bootloader/fact_topologies.json
FACT_TOPOLOGIES=examples/factorial-bootloader/fact_topologies.json
