[package]
name = "deploy"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "deploy"
path = "src/main.rs"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
tokio = { version = "1", features = ["full"] }

